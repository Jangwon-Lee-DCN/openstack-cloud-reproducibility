# Nova extended compute image

This image layers the exact `nova-extended-compute` Git revision over the
immutable Airship Nova 2026.1 base. The queue requires both the reproducibility
and downstream Nova revisions and publishes only the resulting immutable
digest.
