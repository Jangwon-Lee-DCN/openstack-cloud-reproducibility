---
- name: Validate and prepare OpenStack provider uplinks
  hosts: all
  become: true
  gather_facts: true
  roles:
    - role: provider_uplink
      when: "'compute' in (node_roles | default([])) or 'ovn_gateway' in (node_roles | default([]))"
