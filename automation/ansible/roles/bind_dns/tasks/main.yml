---
- name: Run the rack-aware DNS contract
  ansible.builtin.include_tasks: rack.yml
  when: dns_reverse_zones is defined

- name: Run the legacy single-zone DNS contract
  ansible.builtin.include_tasks: legacy.yml
  when: dns_reverse_zones is not defined
