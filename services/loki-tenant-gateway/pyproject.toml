[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dcn-loki-tenant-gateway"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi==0.116.1", "httpx==0.28.1", "uvicorn==0.35.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
