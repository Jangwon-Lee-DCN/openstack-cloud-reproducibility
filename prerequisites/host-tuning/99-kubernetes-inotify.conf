# Kubernetes nodes run many controllers and log watchers under the root UID.
# The Ubuntu default of 128 instances was exhausted on controller-0.
fs.inotify.max_user_instances = 1024
fs.inotify.max_user_watches = 1048576
fs.inotify.max_queued_events = 65536
