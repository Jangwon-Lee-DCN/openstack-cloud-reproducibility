# OpenStack-Helm runs QEMU with the numeric kvm group from its container image.
# That GID is not guaranteed to match the host's kvm group GID. Permit QEMU in
# the privileged libvirt pod to open KVM after every host boot.
KERNEL=="kvm", MODE="0666", OPTIONS+="static_node=kvm"
