# Libvirt runs on all accepted compute nodes. Block storage is provided by
# PowerStore through Cinder, so no Ceph configmap or keyring may be mounted.
conf:
  ceph:
    enabled: false

pod:
  tolerations:
    libvirt:
      enabled: true
