# Site placement override for the pinned OpenStack-Helm Cinder chart.
# Control-plane components retain the upstream openstack-control-plane selector.
# Only the cinder-volume workload is restricted to cloud-controller-0.
labels:
  volume:
    node_selector_key: openstack-cinder-volume
    node_selector_value: enabled
  backup:
    node_selector_key: openstack-cinder-volume
    node_selector_value: enabled
pod:
  tolerations:
    cinder:
      enabled: true
  replicas:
    api: 2
    scheduler: 2
    volume: 1
    backup: 1
  lifecycle:
    disruption_budget:
      api:
        min_available: 1
images:
  tags:
    rabbit_init: quay.io/airshipit/rabbitmq:3.10.18-management
conf:
  # Read-only cloud inventory and capacity access for the monitoring role.
  policy:
    "volume_extension:services:index": "role:admin or role:monitoring"
    "scheduler_extension:scheduler_stats:get_pools": "role:admin or role:monitoring"
    "volume_extension:quotas:show": "role:admin or role:monitoring"
    "volume:get_all": "role:admin or role:monitoring"
    "volume:get_all_snapshots": "role:admin or role:monitoring"
  ceph:
    pools:
      backup:
        app_name: rbd
        replication: 1
      cinder.volumes:
        app_name: rbd
        replication: 1
  cinder:
    DEFAULT:
      backup_driver: cinder.backup.drivers.ceph.CephBackupDriver
endpoints:
  identity:
    auth:
      admin:
        region_name: RegionOne
        project_name: admin
  volumev3:
    host_fqdn_override:
      public:
        host: cloud.dcn.ssu.ac.kr
    path:
      public: /volume/v3
    scheme:
      public: https
    port:
      api:
        public: 443
manifests:
  ingress_api: false
  service_ingress_api: false
