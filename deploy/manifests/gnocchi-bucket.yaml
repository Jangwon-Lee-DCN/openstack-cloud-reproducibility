apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: gnocchi-metrics
  namespace: openstack
spec:
  generateBucketName: gnocchi-metrics
  storageClassName: rook-ceph-bucket-retain

